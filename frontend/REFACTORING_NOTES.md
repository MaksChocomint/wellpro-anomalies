# Декомпозиция page.tsx - Документация

## Обзор

Файл `page.tsx` был успешно рефакторен с 835 строк до компактной версии (~280 строк) путем разделения логики на переиспользуемые компоненты, кастомные хуки и утилиты.

## Структура после рефакторинга

```
frontend/
├── app/
│   └── page.tsx          (280 строк) - Основной компонент
├── components/
│   ├── AnomalyModal.tsx   (существующий)
│   ├── StatusDisplay.tsx  (существующий)
│   ├── GraphControls.tsx  (существующий)
│   ├── LoadingOverlay.tsx (существующий)
│   ├── AnalysisMethodSelector.tsx    (НОВЫЙ - 46 строк)
│   ├── GraphGrid.tsx                 (НОВЫЙ - 90 строк)
│   └── ControlButtons.tsx            (НОВЫЙ - 48 строк)
├── hooks/
│   ├── useWebSocket.ts     (НОВЫЙ - 145 строк)
│   ├── useDataSimulation.ts (НОВЫЙ - 68 строк)
│   ├── useThresholds.ts     (НОВЫЙ - 67 строк)
│   └── index.ts             (НОВЫЙ - индекс хуков)
└── utils/
    ├── utils.ts            (существующий)
    ├── fileUtils.ts        (НОВЫЙ - 47 строк)
    └── thresholdUtils.ts   (НОВЫЙ - 43 строк)
```

## Компоненты

### AnalysisMethodSelector.tsx

Выбор метода анализа и управление порогами для каждого метода.

- **Props**: analysisMethod, thresholds, onMethodChange, onThresholdChange, isDisabled
- **Функционал**: Динамически отображает поля ввода в зависимости от выбранного метода

### GraphGrid.tsx

Сетка графиков со всеми параметрами и точками аномалий.

- **Props**: liveData, availableParameters, graphVisibility, anomalyInfo
- **Функционал**: Отрисовка интерактивных графиков Plotly

### ControlButtons.tsx

Кнопки управления: загрузка файлов, симуляция, режим real-time.

- **Props**: isSimulationActive, hasLoadedData, isDisabled, обработчики событий
- **Функционал**: Управление файлами и режимами работы приложения

## Кастомные хуки

### useWebSocket.ts

Управление WebSocket соединением и получением данных в реальном времени.

- **Функции**: connectWebSocket, управление сообщениями, обработка аномалий
- **Возвращает**: { wsRef, connectWebSocket }

### useDataSimulation.ts

Симуляция потока данных из загруженного файла.

- **Функции**: startDataSimulation, stopSimulation
- **Возвращает**: { fullDataRef, intervalRef, dataIndexRef, stopSimulation, startDataSimulation }

### useThresholds.ts

Управление порогами анализа для разных методов.

- **Функции**: handleThresholdChange, getThresholdValue, buildMessageForMethod
- **Возвращает**: { thresholdsRef, handleThresholdChange, getThresholdValue, buildMessageForMethod }

## Утилиты

### fileUtils.ts

Функции для работы с файлами:

- `analyzeFile(file, params)` - Загрузка и анализ файла через API
- `extractFlightStartTimeFromFile(fileContent)` - Извлечение времени начала бурения

### thresholdUtils.ts

Функции для работы с порогами:

- `buildParametersMessage(method, thresholds)` - Формирование сообщения для отправки на сервер
- `getThresholdKeysForMethod(method)` - Получение ключей порогов для метода
- `getThresholdLabel(key)` - Получение метки для поля порога

## Преимущества рефакторинга

### ✅ Улучшения

1. **Модульность**: Каждый компонент и хук отвечает за одну задачу
2. **Переиспользование**: Хуки можно применять в других компонентах
3. **Тестируемость**: Отдельные утилиты и хуки легче тестировать
4. **Поддерживаемость**: Код более читаемый и организованный
5. **Снижение сложности**: page.tsx снизился с 835 до 280 строк (67% сокращение)
6. **Лучшее разделение забот**: Логика отделена от представления

### 📊 Метрики

- **Строк в page.tsx**: 835 → 280 (-67%)
- **Новых файлов**: 6 (компоненты и хуки)
- **Новых утилит**: 2
- **Кода в компонентах**: ~184 строки
- **Кода в хуках**: ~280 строк
- **Кода в утилитах**: ~90 строк

## Использование

Все компоненты и хуки готовы к использованию:

```typescript
// В page.tsx или других компонентах
import { useWebSocket } from "@/hooks/useWebSocket";
import { AnalysisMethodSelector } from "@/components/AnalysisMethodSelector";
import { analyzeFile } from "@/utils/fileUtils";
```

## Миграция

Если вы используете старый код page.tsx, просто замените весь файл на новую версию. Все функции остались тем же самым, но теперь разбиты логически.
